import { useEffect, useState } from 'react';import Header from './components/Header';import Hero from './components/Hero';import BurgerStory from './components/BurgerStory';import Campaign from './components/Campaign';import MenuSection from './components/MenuSection';import Ingredients from './components/Ingredients';import FooterCTA from './components/FooterCTA';import CartDrawer from './components/CartDrawer';import AdminPanel from './components/AdminPanel';import AdminAnalytics from './components/AdminAnalytics';import OrderTracker from './components/OrderTracker';import StoreSwitch from './components/StoreSwitch';import { products } from './data';import { saveOrder } from './lib/orders';
const fileData=file=>new Promise(resolve=>{if(!file)return resolve(null);const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.readAsDataURL(file)});
export default function App(){const[cart,setCart]=useState([]),[open,setOpen]=useState(false),[trackOpen,setTrackOpen]=useState(false),[storeOpen,setStoreOpen]=useState(()=>localStorage.getItem('shet-store-open')!=='false'),[orders,setOrders]=useState(()=>JSON.parse(localStorage.getItem('shet-demo-orders')||'[]')),[stock,setStock]=useState(()=>{const base=Object.fromEntries(products.map(product=>[product.id,true]));return {...base,...JSON.parse(localStorage.getItem('shet-demo-stock')||'{}')}});const latestOrder=orders[0]||null;useEffect(()=>{localStorage.setItem('shet-demo-orders',JSON.stringify(orders))},[orders]);useEffect(()=>{localStorage.setItem('shet-demo-stock',JSON.stringify(stock))},[stock]);useEffect(()=>{localStorage.setItem('shet-store-open',String(storeOpen))},[storeOpen]);const statusUpdate=(id,status,stage)=>setOrders(items=>items.map(order=>order.id===id?{...order,status,stage}:order));const deleteOrder=id=>setOrders(items=>items.filter(order=>order.id!==id));if(window.location.pathname==='/admin/analytics')return <AdminAnalytics orders={orders}/>;if(window.location.pathname==='/admin')return <><AdminPanel products={products} stock={stock} onToggleStock={id=>setStock(items=>({...items,[id]:!items[id]}))} orders={orders} onUpdateOrder={statusUpdate} onDeleteOrder={deleteOrder}/><StoreSwitch active={storeOpen} onToggle={()=>setStoreOpen(value=>!value)}/></>;const add=(product,variant)=>{if(!storeOpen||stock[product.id]===false)return;const selected={label:variant[0],price:Number(variant[1].replace(/\D/g,''))},key=`${product.id}-${selected.label}`;setCart(items=>{const found=items.find(item=>item.key===key);return found?items.map(item=>item.key===key?{...item,quantity:item.quantity+1}:item):[...items,{key,product,variant:selected,quantity:1}]});setOpen(true)};const change=(key,quantity)=>setCart(items=>quantity<=0?items.filter(item=>item.key!==key):items.map(item=>item.key===key?{...item,quantity}:item));const confirm=async details=>{const preview=await fileData(details.receipt),order={order_number:String(Date.now()).slice(-8),items:cart.map(item=>({product:item.product,quantity:item.quantity,price:item.variant.price,variant:item.variant.label})),total:details.total,fulfillment:details.mode,customer_name:details.form.name,customer_phone:details.form.phone,address:details.form.address,status:'Pedido recibido',stage:0,receipt_name:details.receipt?.name||'',receipt_preview:preview};const result=await saveOrder(order,details.receipt);if(result.error&&result.error.message!=='Supabase no configurado')throw result.error;const localOrder={...order,id:order.order_number,mode:order.fulfillment,customer:details.form,receipt_path:result.data?.receipt_path||null};setOrders(items=>[localOrder,...items]);setCart([])};const count=cart.reduce((sum,item)=>sum+item.quantity,0);return <><Header cartCount={count} onCart={()=>setOpen(true)} latestOrder={latestOrder} onTrack={()=>setTrackOpen(true)}/><main><Hero onOrder={()=>setOpen(true)}/><BurgerStory/><Campaign/><MenuSection onAdd={add} stock={stock} storeOpen={storeOpen}/><Ingredients/><FooterCTA onOrder={()=>setOpen(true)}/></main>{open&&<CartDrawer cart={cart} onClose={()=>setOpen(false)} onChange={change} onClear={()=>setCart([])} onConfirm={confirm}/>} {trackOpen&&<OrderTracker order={latestOrder} onClose={()=>setTrackOpen(false)}/>}</>}
